export default function ProjectCardRoot({ children }: { children: React.ReactNode }) {
  return (
    <div className="w-full rounded-md p-3 bg-khaki text-faux-black">
      {children}
    </div>
  )
}
